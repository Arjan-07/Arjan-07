Hi there 👋 <br>
This is now an updated git demo file with new line

<!
    git clone (link) -- to open remote (github) file in local and change (code) <br>
    git push origin main -- to upload changes/additions at local to remote(github)  <br>
    cd (folder)---  change directory lets you open a different folder and move to it from previous one  <br>
    ls --- lists the files in the opened folder  <br>
    ls -Hidden lets you access hidden files only through terminal  <br>
    if newfile added -- first add (become staged), then commit (becomes unchanged) to track changes  <br>
    git status -- to check files status in git   <br>
    git add . -- to add all changes at once 

!>
